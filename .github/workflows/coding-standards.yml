name: Coding Standards

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    
    # Configura permisos aquí
    permissions:
      contents: write
    steps:
      uses: laravel/.github/.github/workflows/coding-standards.yml@main